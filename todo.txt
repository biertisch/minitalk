check the norm;
check memory leaks;
do a mock evaluation;

explain concepts and functions:
	UNIX signals
	SIGUSR1 and SIGUSR2
	PID
	problem: velocity
	problem: desynchronization
	unicode characters
	
	sigaction (including struct and flags)
	sigemptyset
	sigaddset(?)
	kill
	getpid
	pause
	usleep
	exit